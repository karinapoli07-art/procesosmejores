---
// Sección todavía no publicada: página en blanco con una sola palabra.
---
<section class="proximamente">
  <h1>PRÓXIMAMENTE!</h1>
</section>

<style>
  .proximamente {
    min-height: 62vh;
    display: grid;
    place-items: center;
    padding: clamp(80px, 14vw, 160px) var(--gutter);
    text-align: center;
  }
  h1 {
    font-size: clamp(2rem, 7vw, 4.5rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    color: var(--ink);
    margin: 0;
  }
</style>
