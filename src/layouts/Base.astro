---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import WhatsApp from '../components/WhatsApp.astro';

interface Props {
  title: string;
  description: string;
  jsonLd?: object[];
  /** true en páginas que no deben aparecer en buscadores (ej. /gracias). */
  noindex?: boolean;
}

const { title, description, jsonLd = [], noindex = false } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site);
---
<!doctype html>
<html lang="es-AR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    {noindex && <meta name="robots" content="noindex, follow" />}
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="sitemap" href="/sitemap-index.xml" />

    <meta property="og:type" content="website" />
    <meta property="og:locale" content="es_AR" />
    <meta property="og:site_name" content="Procesos Mejores" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={new URL('/og.png', Astro.site)} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="Procesos Mejores. Sistemas de gestión que tu equipo sabe sostener." />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={new URL('/og.png', Astro.site)} />

    {jsonLd.map((data) => (
      <script type="application/ld+json" set:html={JSON.stringify(data)} />
    ))}
  </head>
  <body>
    <a class="skip" href="#contenido">Saltar al contenido</a>
    <Header />
    <main id="contenido">
      <slot />
    </main>
    <Footer />
    <WhatsApp />
    <script>
      // Aparición suave de las secciones al hacer scroll. Sólo afecta a las que todavía no se ven al
      // cargar; sin JavaScript o con «reducir movimiento» activado, todo se ve como siempre.
      if ('IntersectionObserver' in window && !matchMedia('(prefers-reduced-motion: reduce)').matches) {
        const observador = new IntersectionObserver((entradas) => {
          for (const e of entradas) {
            if (e.isIntersecting) { e.target.classList.add('is-visible'); observador.unobserve(e.target); }
          }
        }, { rootMargin: '0px 0px -8% 0px' });
        document.querySelectorAll<HTMLElement>('main > section').forEach((s) => {
          if (s.getBoundingClientRect().top < innerHeight) return;
          s.classList.add('reveal');
          observador.observe(s);
        });
      }
    </script>
  </body>
</html>

<style>
  .skip {
    position: absolute; left: 12px; top: -48px; z-index: 100;
    background: var(--ink); color: #fff; padding: 10px 14px; border-radius: 6px;
    text-decoration: none; font-weight: 600;
  }
  .skip:focus { top: 12px; }
</style>
